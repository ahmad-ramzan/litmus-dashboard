services:
  - type: web
    name: litmus-api
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/main
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: false
